# LLM Pipeline

六层模型架构的命令行工具，支持灵活指定每个组件的模型和提示语。

## 架构

```
用户输入 → Assign → Core → Filter → Tool → Check → Show → 用户
              |                                    |
              └→ 解析用户意图，自动配置其他模型的提示语  └→ 检查失败，返回Core重生成
```

### 各层职责

| 层级 | 职责 | 数据来源 | `{content}` 含义 |
|-----|------|---------|----------------|
| **Assign** | 解析用户意图，生成/修改其他模型的提示语 | 无 | 用户输入 |
| **Core** | 生成初始内容 | 无 | 用户原始输入 |
| **Filter** | 事实核查，过滤错误信息 | `database.json`（知识库） | Core 输出 |
| **Tool** | 执行工具调用（Agent）【可选】 | 外部API/对话Agent | Filter 输出 |
| **Check** | 目标达成检查、合规检查，屏蔽词过滤 | `rules.json`（规则库） | Tool 输出 |
| **Show** | 格式转换，语气调整 | 无 | Check 输出 |

## 核心特性：并集与交集

每个层级支持**并集（并行）**和**交集（串行）**执行：

### 并集执行（空格分隔）
多个模型**同时并行执行**，结果合并：
```bash
--filter-model deepseek phi3 qwen
```
执行流程：`content → [Filter(deepseek) ∥ Filter(phi3) ∥ Filter(qwen)] → 结果合并`

### 交集执行（多次参数）
多个模型**依次串行执行**，前一个输出作为后一个输入：
```bash
--filter-model deepseek --filter-model phi3
```
执行流程：`content → Filter(deepseek) → Filter(phi3) → 输出`

### 单个执行
单个模型正常执行：
```bash
--filter-model deepseek
```
执行流程：`content → Filter(deepseek) → 输出`

### 判断规则

| 参数形式 | 类型 | 执行方式 | 示例 |
|---------|------|--------|------|
| `--model-name m1` | 单个 | 执行1次 | 单个Filter |
| `--model-name m1 m2 m3` | 并集 | 并行执行，合并结果 | 多个Filter同时执行 |
| `--model-name m1 --model-name m2` | 交集 | 串行执行 | Filter1 → Filter2 |

## 安装

```bash
pip install requests pyyaml
```

## 快速开始

### 1. 查看可用模型

```bash
python3 run.py --list
```

### 2. 仅使用 Core 模型（最简单）

```bash
python3 run.py \
  --core-model deepseek \
  --core-prompt "你是一个专业的AI助手，请回答用户的问题：{content}" \
  --prompt "你好"
```

### 3. 单个 Filter + 单个 Check

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model deepseek \
  --check-model deepseek \
  --show-model deepseek \
  --prompt "你好"
```

### 4. Filter 并集执行（多个模型并行）

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model deepseek phi3 qwen \
  --check-model deepseek \
  --show-model deepseek \
  --prompt "你好"
```

### 5. Filter 交集执行（多个模型串行）

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model deepseek --filter-model phi3 --filter-model qwen \
  --check-model deepseek \
  --show-model deepseek \
  --prompt "你好"
```

### 6. 复杂组合

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model math-filter history-filter \
  --tool-model weather-api --tool-model search-api \
  --check-model political-check --check-model commercial-check \
  --show-model translate-english friendly-tone \
  --prompt "北京今天天气怎么样？"
```

## 命令参数

| 参数 | 说明 | 是否必需 |
|-----|------|---------|
| `--models` | 模型配置文件（YAML格式） | 否（默认 models.yaml） |
| `--datas` | 知识数据库文件（JSON格式） | 否 |
| `--rules` | 合规规则文件（JSON格式） | 否 |
| `--assign-model` | Assign使用的模型名称 | 否 |
| `--assign-prompt` | Assign的提示语 | 否 |
| `--core-model` | Core使用的模型名称 | **是** |
| `--core-prompt` | Core的提示语 | 否（默认 `{content}`） |
| `--filter-model` | Filter使用的模型【支持并集/交集】 | 否 |
| `--filter-prompt` | Filter的提示语模板 | 否 |
| `--tool-model` | Tool使用的模型【支持并集/交集】 | 否 |
| `--tool-prompt` | Tool的提示语模板 | 否 |
| `--check-model` | Check使用的模型【支持并集/交集】 | 否 |
| `--check-prompt` | Check的提示语模板 | 否 |
| `--show-model` | Show使用的模型【支持并集/交集】 | 否 |
| `--show-prompt` | Show的提示语模板 | 否 |
| `--prompt` | 用户输入 | **是** |
| `--json` | JSON格式输出 | 否 |
| `--list` | 列出所有可用模型 | 否 |

### 并集/交集参数说明

| 层级 | 并集（空格分隔） | 交集（多次参数） |
|-----|----------------|----------------|
| Filter | `--filter-model m1 m2 m3` | `--filter-model m1 --filter-model m2` |
| Tool | `--tool-model t1 t2 t3` | `--tool-model t1 --tool-model t2` |
| Check | `--check-model c1 c2 c3` | `--check-model c1 --check-model c2` |
| Show | `--show-model s1 s2 s3` | `--show-model s1 --show-model s2` |

## 配置文件格式

### models.yaml

```yaml
models:
  deepseek:
    type: api
    url: https://api.deepseek.com/v1/chat/completions
    key: YOUR_API_KEY
    model: deepseek-chat
  phi3:
    type: api
    url: http://localhost:11434/api/chat
    model: phi3
```

### database.json

```json
{
  "facts": [
    "水的沸点是100摄氏度",
    "法国的首都是巴黎",
    "中国的首都是北京"
  ]
}
```

### rules.json

```json
{
  "rules": [
    "不得讨论中国领导人及其家属的负面信息",
    "不得传播未经证实的信息"
  ],
  "banned_keywords": [
    "台独",
    "港独",
    "暴力",
    "色情"
  ]
}
```

## 工作流程示例

### 场景1：单纯对话（不使用 Tool）

```
用户输入 → Core → Filter → Check → Show → 用户
```

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model deepseek \
  --check-model deepseek \
  --show-model deepseek \
  --prompt "你好，今天心情不错"
```

### 场景2：Filter 并集（多领域同时核查）

```
用户输入 → Core → [Filter1 ∥ Filter2 ∥ Filter3] → Check → Show → 用户
```

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model math-filter history-filter science-filter \
  --check-model deepseek \
  --show-model deepseek \
  --prompt "法国的首都是哪里？"
```

### 场景3：Check 交集（多维度串行检查）

```
用户输入 → Core → Filter → [Check1 → Check2 → Check3] → Show → 用户
```

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model deepseek \
  --check-model political-check --check-model commercial-check --check-model privacy-check \
  --show-model deepseek \
  --prompt "分析这个商业提案"
```

### 场景4：混合场景

```
用户输入 → Core → [F1 ∥ F2] → [T1 → T2] → [C1 ∥ C2] → [S1 → S2] → 用户
```

```bash
python3 run.py \
  --core-model deepseek \
  --filter-model math-filter history-filter \
  --tool-model search-api --tool-model calculator-api \
  --check-model political-check commercial-check \
  --show-model translate-english friendly-tone \
  --prompt "分析这笔投资的回报率"
```

## 特点

1. **灵活配置**：每个模型都可以独立指定
2. **并集执行**：支持多个模型并行执行，提高效率
3. **交集执行**：支持多个模型串行执行，实现二次过滤
4. **自动检测**：自动识别参数形式（单个/并集/交集）
5. **并行优化**：并集执行使用线程池并行处理
6. **自动重试**：Check失败时自动返回Core重生成